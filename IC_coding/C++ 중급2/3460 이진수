#include < iostream >

using namespace std;

int main(void) {

	int tc;
	cin >> tc;
	for (int i = 0; i < tc; i++) {
		int n;
		cin >> n;

		int idx = 0;
		while (n > 0) {
			if(n % 2 == 1) {
				cout << idx << ' ';
			}
			n = n / 2;
			idx++;
		}
		cout << endl;
	}

	return 0;
}
